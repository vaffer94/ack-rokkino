# Copia questo file in ".env" (nella stessa cartella) e inserisci i valori
# reali. ".env" è nel .gitignore e non va mai committato.

# Bot Telegram per il promemoria "chiudi la finestra" (vedi noteVF.md per
# come crearlo con @BotFather e trovare il chat_id).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
